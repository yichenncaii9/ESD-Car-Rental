---
phase: 05-async-workers
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/verify_phase5.sh
  - websocket_server/app.py
autonomous: true
requirements:
  - WS-01
  - WS-03
  - WS-04

must_haves:
  truths:
    - "websocket_server /health returns 200 on port 6100"
    - "POST /notify emits a Socket.IO report_update event to connected clients"
    - "The emitted payload includes both 'id' (aliased from report_id) and all input fields"
    - "verify_phase5.sh exists and tests WS-01 through WORK-07 smoke checks"
  artifacts:
    - path: "websocket_server/app.py"
      provides: "POST /notify with socketio.emit('report_update', {**data, 'id': data['report_id']})"
      contains: "socketio.emit"
    - path: "scripts/verify_phase5.sh"
      provides: "Smoke test script for Phase 5 verification"
      contains: "verify_phase5"
  key_links:
    - from: "websocket_server/app.py /notify"
      to: "ServiceDashboardView socket.on('report_update')"
      via: "socketio.emit('report_update', payload)"
      pattern: "socketio\\.emit.*report_update"
---

<objective>
Upgrade the websocket_server stub to emit real Socket.IO events, and create the Phase 5 smoke-test script.

Purpose: websocket_server is the convergence point for both AMQP workers — it must work before workers can be tested. Also creates verify_phase5.sh so all subsequent tasks have automated verification.
Output: websocket_server/app.py with real /notify body; scripts/verify_phase5.sh smoke tests.
</objective>

<execution_context>
@/Users/chaiyichen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chaiyichen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-async-workers/05-CONTEXT.md
@.planning/phases/05-async-workers/05-RESEARCH.md

<interfaces>
<!-- Current websocket_server/app.py stub (Phase 1) — full replacement below -->
<!-- Key: /notify route must call socketio.emit, not return stub message -->

From websocket_server/app.py (Phase 1 stub):
```python
@app.route("/notify", methods=["POST"])
def notify():
    # Phase 1 stub — real Socket.IO emit in Phase 5
    data = request.get_json() or {}
    print(f"[websocket_server] /notify received: {data}")
    return jsonify({"status": "ok", "message": "Phase 1 stub"}), 200
```

CRITICAL — ServiceDashboardView line 97 match logic (frontend/src/views/ServiceDashboardView.vue):
  findIndex(r => r.id === data.report_id || r.id === data.id)
Without "id" field in emit, findIndex returns -1 and orphan rows appear on every event.

Locked emit shape (from 05-CONTEXT.md):
```python
socketio.emit("report_update", {
    **data,
    "id": data.get("report_id")   # REQUIRED for frontend findIndex
})
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create scripts/verify_phase5.sh smoke test script</name>
  <files>scripts/verify_phase5.sh</files>
  <action>
Create `scripts/verify_phase5.sh` as a bash smoke-test script for Phase 5. The script uses curl and docker-compose log grep checks.

Structure:
1. Header: `#!/usr/bin/env bash` with `set -euo pipefail`. Print "=== Phase 5 Smoke Tests ===" on start.
2. PASS/FAIL counters initialized to 0.
3. Helper function `check` that takes a label and runs a command — prints PASS or FAIL and increments counters.
4. Helper function `log_check` that takes a label and a grep pattern to run against `docker-compose logs --no-color --tail=100 <service>`.
5. Tests to include (in order):

   WS-01: websocket_server health check
   `curl -sf http://localhost:6100/health`

   WS-03: POST /notify returns 200
   `curl -sf -X POST http://localhost:6100/notify -H "Content-Type: application/json" -d '{"report_id":"smoke-001","event":"sms_sent","severity":"high","message":"smoke test"}'`

   WORK-01: twilio_wrapper queue binding (log check)
   Pattern: "Waiting for messages" in `docker-compose logs --no-color --tail=100 twilio_wrapper`

   WORK-07 (twilio): RabbitMQ connection (log check)
   Pattern: "Connected to RabbitMQ" in `docker-compose logs --no-color --tail=100 twilio_wrapper`

   WORK-02: Service team SMS sent (log check)
   Pattern: "Service team SMS sent" in `docker-compose logs --no-color --tail=100 twilio_wrapper`

   WORK-05: activity_log queue binding (log check)
   Pattern: "Waiting for messages" in `docker-compose logs --no-color --tail=100 activity_log`

   WORK-07 (activity_log): RabbitMQ connection (log check)
   Pattern: "Connected to RabbitMQ" in `docker-compose logs --no-color --tail=100 activity_log`

   WORK-06: Firestore write (log check)
   Pattern: "Firestore write" in `docker-compose logs --no-color --tail=100 activity_log`

   WS-01 container: websocket_server log healthy (log check)
   Pattern: "Client connected\|notify received\|Phase 5" in `docker-compose logs --no-color --tail=100 websocket_server`

6. Final summary: print total PASS and FAIL counts. Exit 1 if FAIL > 0.

The log_check function must use `2>/dev/null` so missing containers don't crash the script. Use `|| true` around docker-compose log checks if container is not running.

Make the file executable with `chmod +x scripts/verify_phase5.sh` (run this as a shell command after writing the file).
  </action>
  <verify>
    <automated>bash /Applications/MAMP/htdocs/y2s2/ESD/ESDProj/scripts/verify_phase5.sh 2>&1 | head -5</automated>
  </verify>
  <done>scripts/verify_phase5.sh exists, is executable, prints "=== Phase 5 Smoke Tests ===" header when run, and exits with meaningful output even when services are not running.</done>
</task>

<task type="auto">
  <name>Task 2: Upgrade websocket_server /notify to emit Socket.IO report_update</name>
  <files>websocket_server/app.py</files>
  <action>
Replace the Phase 1 stub in `websocket_server/app.py`. The file is currently 32 lines. Keep all existing structure (Flask app init, CORS, SocketIO init, /health route, on_connect handler, socketio.run at bottom) and only replace the /notify route body.

Replace the entire /notify function with:
```python
@app.route("/notify", methods=["POST"])
def notify():
    data = request.get_json() or {}
    print(f"[websocket_server] /notify received: {data}")
    socketio.emit("report_update", {
        **data,
        "id": data.get("report_id")   # REQUIRED — ServiceDashboardView findIndex matches r.id === data.id
    })
    return jsonify({"status": "ok"}), 200
```

Do NOT change:
- The imports (flask, flask_cors, flask_socketio, os)
- `app = Flask(__name__)`, `CORS(app)`, `SocketIO(app, cors_allowed_origins="*")`
- The /health route
- The on_connect handler
- `socketio.run(app, ...)` at the bottom

The only change is replacing the stub comment + hardcoded stub response in /notify with the real socketio.emit call.
  </action>
  <verify>
    <automated>curl -sf http://localhost:6100/health 2>/dev/null || echo "SKIP: container not running (verify after docker-compose up)"</automated>
  </verify>
  <done>websocket_server/app.py contains socketio.emit("report_update", {..., "id": data.get("report_id")}). The "Phase 1 stub" comment is removed. POST /notify returns {"status": "ok"} (no "message" field). When running, POST to /notify emits report_update to connected Socket.IO clients.</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `grep -n "socketio.emit" websocket_server/app.py` — must show the emit line
2. `grep -n "Phase 1 stub" websocket_server/app.py` — must return empty (stub removed)
3. `grep -n '"id"' websocket_server/app.py` — must show data.get("report_id") alias
4. `bash scripts/verify_phase5.sh 2>&1 | head -3` — must print Phase 5 header without crashing
5. `ls -la scripts/verify_phase5.sh` — must exist and be executable (x bit set)
</verification>

<success_criteria>
- websocket_server/app.py /notify emits socketio.emit("report_update", {**data, "id": data.get("report_id")})
- POST /notify returns {"status": "ok"} with HTTP 200
- scripts/verify_phase5.sh is executable and covers all 9 smoke checks (WS-01, WS-03, WORK-01 through WORK-07)
- No regression in /health endpoint
</success_criteria>

<output>
After completion, create `.planning/phases/05-async-workers/05-01-SUMMARY.md`
</output>
