---
phase: 03-atomic-services
plan: 03
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - atomic/pricing_service/app.py
  - atomic/driver_service/app.py
autonomous: true
requirements: [PRICE-01, PRICE-02, DRV-01, DRV-02]

must_haves:
  truths:
    - "GET /api/pricing returns hardcoded rates: sedan=12.50, suv=18.00, van=15.00"
    - "GET /api/pricing/calculate?vehicle_type=sedan&hours=2 returns total=25.00"
    - "GET /api/pricing/policy returns the three-tier refund policy tiers array"
    - "GET /api/drivers/test_driver_001 returns the seeded John Tan driver record"
    - "POST /api/drivers/validate with license_number=S1234567A returns valid:true"
    - "POST /api/drivers/validate with expired/unknown license returns valid:false with reason"
  artifacts:
    - path: "atomic/pricing_service/app.py"
      provides: "Pricing calculation and policy endpoints (no Firestore)"
      contains: "RATES = {\"sedan\": 12.50"
    - path: "atomic/driver_service/app.py"
      provides: "Driver lookup by UID and license validation"
      contains: "db.collection(\"drivers\")"
  key_links:
    - from: "atomic/driver_service/app.py"
      to: "Firestore drivers collection"
      via: "db.collection('drivers').where('uid','==',uid).stream()"
      pattern: "where\\(\"uid\""
    - from: "atomic/driver_service/app.py"
      to: "Firestore drivers collection"
      via: "db.collection('drivers').document(license_number).get()"
      pattern: "document\\(license_number\\)"
---

<objective>
Implement two independent atomic services in parallel:
1. pricing_service — pure Python (no Firestore): real calculate endpoint + new /policy endpoint
2. driver_service — Firestore: get driver by UID and validate license

Purpose: pricing_service is called by composite cancel_booking (Phase 4) for the /policy endpoint. driver_service is the first call in the book_car composite flow.
Output: Both app.py files with all stubs replaced by working logic.
</objective>

<execution_context>
@/Users/chaiyichen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chaiyichen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-atomic-services/03-CONTEXT.md
@.planning/phases/03-atomic-services/03-RESEARCH.md

<interfaces>
<!-- pricing_service current stub routes (replace bodies): -->
  GET /api/pricing        → get_pricing()       — already returns correct rates (keep as-is)
  GET /api/pricing/calculate → calculate_pricing() — replace stub body
  NEW: GET /api/pricing/policy → get_pricing_policy() — new route

<!-- driver_service current stub routes (replace bodies): -->
  GET /api/drivers/<string:uid>  → get_driver(uid)
  POST /api/drivers/validate     → validate_driver()

<!-- CRITICAL route ordering for driver_service: -->
  /api/drivers/validate is a static segment — must be registered BEFORE /api/drivers/<uid>
  Current stub already has /api/drivers/<uid> first, then /api/drivers/validate.
  Flask processes POST vs GET differently (different methods), so ordering is safe here,
  but add a comment confirming the reasoning.

<!-- Driver document shape (from seed_data.py): -->
  { uid, name, license_number, license_expiry, email }
  Document ID = license_number (natural key)
  Collection name: "drivers"

<!-- Locked: cancellation policy tiers shape (from CONTEXT.md): -->
  {"tiers": [
    {"hours_before": 24, "refund_percent": 100},
    {"hours_before": 1,  "refund_percent": 50},
    {"hours_before": 0,  "refund_percent": 0}
  ]}

<!-- Locked: driver validation responses (from CONTEXT.md): -->
  { valid: false, reason: "driver not found" }
  { valid: false, reason: "license expired" }
  { valid: true }
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement pricing_service calculate + policy endpoints</name>
  <files>atomic/pricing_service/app.py</files>
  <behavior>
    - GET /api/pricing: returns {"status":"ok","rates":{"sedan":12.50,"suv":18.00,"van":15.00}} — already correct in stub, keep it
    - GET /api/pricing/calculate?vehicle_type=sedan&hours=2: returns {"status":"ok","vehicle_type":"sedan","hours":2.0,"total":25.0}
    - GET /api/pricing/calculate?vehicle_type=suv&hours=1.5: returns total=27.0
    - GET /api/pricing/calculate?vehicle_type=van&hours=3: returns total=45.0
    - GET /api/pricing/calculate with unknown vehicle_type: returns 400 {"status":"error","message":"Invalid vehicle_type"}
    - GET /api/pricing/calculate with non-numeric hours: returns 400 {"status":"error","message":"Invalid hours"}
    - GET /api/pricing/policy: returns {"status":"ok","tiers":[{"hours_before":24,"refund_percent":100},{"hours_before":1,"refund_percent":50},{"hours_before":0,"refund_percent":0}]}
  </behavior>
  <action>
Rewrite atomic/pricing_service/app.py. No Firestore — pricing is pure Python.

Add `from flask import Flask, jsonify, request` (add `request`).

Define RATES dict at module level (after imports, before routes):
```python
RATES = {"sedan": 12.50, "suv": 18.00, "van": 15.00}
```

**GET /api/pricing** — keep existing stub body exactly (it already returns the correct rates dict).

**GET /api/pricing/calculate** (replace stub body):
- `vehicle_type = request.args.get("vehicle_type", "").lower()`
- `hours_str = request.args.get("hours", "")`
- If vehicle_type not in RATES: return 400 {"status":"error","message":"Invalid vehicle_type"}
- Try `hours = float(hours_str)` — except ValueError/TypeError: return 400 {"status":"error","message":"Invalid hours"}
- `total = round(RATES[vehicle_type] * hours, 2)`
- Return 200 {"status":"ok","vehicle_type":vehicle_type,"hours":hours,"total":total}

**GET /api/pricing/policy** (NEW route — add after calculate):
- Return 200 with the exact locked shape from CONTEXT.md:
  ```python
  return jsonify({
      "status": "ok",
      "tiers": [
          {"hours_before": 24, "refund_percent": 100},
          {"hours_before": 1,  "refund_percent": 50},
          {"hours_before": 0,  "refund_percent": 0}
      ]
  }), 200
  ```

Keep /health and `if __name__ == "__main__":` unchanged.
  </action>
  <verify>
    <automated>bash /Applications/MAMP/htdocs/y2s2/ESD/ESDProj/verify_phase3.sh 2>&amp;1 | grep -E "^  (PASS|FAIL): PRICE"</automated>
  </verify>
  <done>PRICE-01 and PRICE-02 checks in verify_phase3.sh both PASS. GET /api/pricing/policy returns the three-tier tiers array (tested manually via curl -s http://localhost:5005/api/pricing/policy).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement driver_service Firestore routes</name>
  <files>atomic/driver_service/app.py</files>
  <behavior>
    - GET /api/drivers/test_driver_001: returns {"status":"ok","data":{"uid":"test_driver_001","name":"John Tan","license_number":"S1234567A","license_expiry":"2027-12-31","email":"john.tan@example.com"}}
    - GET /api/drivers/unknown_uid: returns 404 {"status":"error","message":"Driver not found"}
    - POST /api/drivers/validate with {"license_number":"S1234567A"}: returns {"valid":true}
    - POST /api/drivers/validate with {"license_number":"EXPIRED123"} (expired): returns {"valid":false,"reason":"license expired"}
    - POST /api/drivers/validate with {"license_number":"NOTEXIST"}: returns {"valid":false,"reason":"driver not found"}
    - POST /api/drivers/validate with missing license_number: returns 400 {"status":"error","message":"Missing required field: license_number"}
  </behavior>
  <action>
Rewrite atomic/driver_service/app.py. Keep Firestore init block exactly as-is.

Add imports at top: `from flask import Flask, jsonify, request` and `from datetime import date`.

**GET /api/drivers/<uid>** (replace stub body):
- Guard: if db is None, return 500
- `docs = list(db.collection("drivers").where("uid", "==", uid).stream())`
- CRITICAL: drivers are keyed by license_number (not uid); must use .where() query, NOT .document(uid)
- If not docs: return 404 {"status":"error","message":"Driver not found"}
- Return 200 {"status":"ok","data":docs[0].to_dict()}

**POST /api/drivers/validate** (replace stub body):
- Guard: if db is None, return 500
- `body = request.get_json(silent=True) or {}`
- `license_number = body.get("license_number")`
- If not license_number: return 400 {"status":"error","message":"Missing required field: license_number"}
- `doc = db.collection("drivers").document(license_number).get()`
- If not doc.exists: return 200 {"valid":False,"reason":"driver not found"}
  (Note: validate returns 200 even for invalid — it is a validation query, not a resource lookup)
- `driver = doc.to_dict()`
- `expiry = date.fromisoformat(driver["license_expiry"])`
- If expiry <= date.today(): return 200 {"valid":False,"reason":"license expired"}
- Return 200 {"valid":True}

Route ordering note: GET /api/drivers/<uid> and POST /api/drivers/validate use different HTTP methods (GET vs POST), so Flask can distinguish them regardless of order. Keep existing stub order. Add comment: "# GET vs POST — method-based dispatch; no ordering conflict".

Keep /health and `if __name__ == "__main__":` unchanged.
  </action>
  <verify>
    <automated>bash /Applications/MAMP/htdocs/y2s2/ESD/ESDProj/verify_phase3.sh 2>&amp;1 | grep -E "^  (PASS|FAIL): DRV"</automated>
  </verify>
  <done>DRV-01 and DRV-02 checks in verify_phase3.sh both PASS. GET /api/drivers/test_driver_001 returns John Tan's record, POST /api/drivers/validate with S1234567A returns valid:true.</done>
</task>

</tasks>

<verification>
bash verify_phase3.sh shows PASS for PRICE-01, PRICE-02, DRV-01, DRV-02. No FAIL for any of these four checks.
</verification>

<success_criteria>
- atomic/pricing_service/app.py: calculate route computes correctly, policy route returns three tiers, no "Phase 1 stub" in route bodies
- atomic/driver_service/app.py: get_driver uses .where("uid") query, validate_driver uses .document(license_number), date comparison uses date.fromisoformat()
- verify_phase3.sh PRICE-01, PRICE-02, DRV-01, DRV-02 all PASS
</success_criteria>

<output>
After completion, create `.planning/phases/03-atomic-services/03-03-SUMMARY.md`
</output>
