---
phase: 02-frontend
plan: "07"
type: execute
wave: 4
depends_on:
  - 02-04
files_modified:
  - .planning/REQUIREMENTS.md
autonomous: true
gap_closure: true
requirements:
  - WS-02

must_haves:
  truths:
    - "WS-02 description in REQUIREMENTS.md reflects ServiceDashboardView as the Socket.IO connection view"
    - "WS-02 traceability row notes Phase 2 (client) and Phase 5 (server) both contribute"
    - "No plan in Phase 2 leaves WS-02 unclaimed — this plan's requirements frontmatter claims WS-02"
  artifacts:
    - path: .planning/REQUIREMENTS.md
      provides: Authoritative WS-02 requirement text matching the implemented behavior
      contains: "WS-02"
  key_links:
    - from: .planning/REQUIREMENTS.md (WS-02)
      to: frontend/src/views/ServiceDashboardView.vue
      via: Requirement text specifies ServiceDashboard
      pattern: "WS-02.*ServiceDashboard"
---

<objective>
Resolve the WS-02 traceability gap identified in 02-VERIFICATION.md by updating REQUIREMENTS.md to accurately describe the implemented behavior and close the orphaned-requirement finding.

Purpose: The verification found WS-02 orphaned — no Phase 2 plan claimed it in `requirements:` frontmatter, and the WS-02 text says "ReportIncident view" while the correct implementation (per locked CONTEXT.md decision) places Socket.IO in ServiceDashboardView. This plan closes both issues: it claims WS-02, and it corrects the WS-02 requirement text to match the implementation.

Output: Updated REQUIREMENTS.md with corrected WS-02 description and traceability note. No code changes needed — the implementation in ServiceDashboardView.vue is already correct.
</objective>

<execution_context>
@/Users/chaiyichen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chaiyichen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-frontend/02-CONTEXT.md
@.planning/REQUIREMENTS.md

<interfaces>
<!-- Key facts from Phase 2 implementation and VERIFICATION.md -->

CONTEXT.md locked decision (ServiceDashboard section):
  "Real-time updates via Socket.IO (connects to websocket_server on load of this view)"
  -> "this view" = ServiceDashboardView, not ReportIncidentView

CONTEXT.md specifics section:
  "ServiceDashboard should connect Socket.IO on mount of that view (WS-02)"
  -> CONTEXT.md explicitly ties WS-02 to ServiceDashboard

Verification finding:
  - WS-02 text says "ReportIncident view" but impl is in ServiceDashboardView
  - Verifier notes option (b) — updating WS-02 to ServiceDashboard — is "operationally more correct"
  - ServiceDashboardView.vue: io('http://localhost:6100'), socket.on('report_update'), onUnmounted disconnect
  - Plan 02-04 notes "WS-02 partial — server side is Phase 5" in its objective but did NOT claim WS-02 in frontmatter

Current WS-02 text (line 87 in REQUIREMENTS.md):
  "**WS-02**: Vue.js frontend connects to websocket_server via Socket.IO on load of ReportIncident view"

Current WS-02 traceability row (line 196):
  "| WS-02 | Phase 2 - Frontend | Pending |"

Phase 5 ROADMAP entry (success criteria #4):
  "Vue.js ReportIncident view connects to websocket_server on load..."
  NOTE: ROADMAP Phase 5 success criteria #4 also incorrectly says ReportIncident — update this too.

Phase 5 ROADMAP requirements list: WORK-01, WORK-02, WORK-03, WORK-04, WORK-05, WORK-06, WORK-07, WS-01, WS-02, WS-03, WS-04
NOTE: WS-02 appears in Phase 5 ROADMAP requirements AND must remain partially tied to Phase 2 (the client-side Socket.IO connection is Phase 2; the server-side /notify endpoint is Phase 5). The traceability row should capture both.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update WS-02 in REQUIREMENTS.md — correct view name and traceability</name>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
Edit REQUIREMENTS.md to resolve the WS-02 orphan gap. Make the following targeted changes:

1. **Change the WS-02 requirement text** (in the WebSocket section under `### WebSocket`):

   FROM:
   ```
   - [ ] **WS-02**: Vue.js frontend connects to websocket_server via Socket.IO on load of ReportIncident view
   ```

   TO:
   ```
   - [ ] **WS-02**: Vue.js frontend connects to websocket_server via Socket.IO on load of ServiceDashboard view
   ```

   Rationale: CONTEXT.md locked decision explicitly states "ServiceDashboard — Real-time updates via Socket.IO (connects to websocket_server on load of this view)". The original WS-02 text said "ReportIncident" but the ServiceDashboard is the correct view for a live ops dashboard. Implementation in ServiceDashboardView.vue is complete and correct (Phase 2 client side). Phase 5 implements the server-side /notify endpoint.

2. **Update the WS-02 traceability row** (in the `## Traceability` table):

   FROM:
   ```
   | WS-02 | Phase 2 - Frontend | Pending |
   ```

   TO:
   ```
   | WS-02 | Phase 2 - Frontend (client: Socket.IO in ServiceDashboardView) / Phase 5 - Async Workers (server: websocket_server /notify endpoint) | Pending |
   ```

   Rationale: WS-02 has two delivery parts — the frontend Socket.IO client (Phase 2, already implemented) and the websocket_server that Phase 5 will build. The traceability row should reflect both phases.

No other changes to REQUIREMENTS.md. All other requirements, their status markers, and the coverage summary remain unchanged.
  </action>
  <verify>
    <automated>grep -n "WS-02" /Applications/MAMP/htdocs/y2s2/ESD/ESDProj/.planning/REQUIREMENTS.md</automated>
  </verify>
  <done>
- WS-02 requirement text says "ServiceDashboard view" (not "ReportIncident view")
- WS-02 traceability row references both Phase 2 (client) and Phase 5 (server)
- grep output shows no remaining mention of "ReportIncident" on the WS-02 definition line
  </done>
</task>

</tasks>

<verification>
Confirm the WS-02 text is updated:
```bash
grep -A1 "WS-02" /Applications/MAMP/htdocs/y2s2/ESD/ESDProj/.planning/REQUIREMENTS.md
```

Expected: WS-02 line contains "ServiceDashboard" not "ReportIncident"; traceability row mentions Phase 2 and Phase 5.

Confirm no unintended changes:
```bash
grep -c "WS-0" /Applications/MAMP/htdocs/y2s2/ESD/ESDProj/.planning/REQUIREMENTS.md
```

Expected: same count as before (4 WS requirements still present).
</verification>

<success_criteria>
- REQUIREMENTS.md WS-02 text reads "ServiceDashboard view" — matches the Phase 2 implementation
- WS-02 traceability row acknowledges both Phase 2 (client-side Socket.IO) and Phase 5 (server-side /notify)
- This plan's `requirements: [WS-02]` frontmatter closes the orphaned-requirement traceability gap
- No code files are modified — the ServiceDashboardView.vue implementation was already correct
</success_criteria>

<output>
After completion, create `.planning/phases/02-frontend/02-07-SUMMARY.md` following the summary template.

Note for future phases: WS-02 is now shared between Phase 2 (client delivered) and Phase 5 (server to deliver). When Phase 5 plans are created, a plan in that phase must also list WS-02 in its requirements to confirm the server-side delivery. The Socket.IO event name agreed in Phase 2 is `report_update` — Phase 5 websocket_server MUST emit this exact name.
</output>
