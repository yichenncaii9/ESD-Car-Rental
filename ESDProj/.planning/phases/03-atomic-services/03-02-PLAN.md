---
phase: 03-atomic-services
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - atomic/vehicle_service/app.py
autonomous: true
requirements: [VEH-01, VEH-02, VEH-03]

must_haves:
  truths:
    - "GET /api/vehicles returns all 10 seeded vehicles as a list"
    - "GET /api/vehicles/SBA1234A returns the single Toyota Corolla document"
    - "PUT /api/vehicles/SBA1234A/status updates the status field in Firestore"
    - "All three endpoints return correct HTTP status codes (200 success, 404 missing, 400 bad input)"
  artifacts:
    - path: "atomic/vehicle_service/app.py"
      provides: "Fully implemented vehicle_service with Firestore read/write"
      contains: "db.collection(\"vehicles\")"
  key_links:
    - from: "atomic/vehicle_service/app.py"
      to: "Firestore vehicles collection"
      via: "db.collection('vehicles').stream() and .document(id).get()"
      pattern: "db\\.collection\\(\"vehicles\"\\)"
---

<objective>
Replace the three Phase 1 stubs in vehicle_service/app.py with real Firestore read/write logic.

Purpose: vehicle_service is the first service the frontend calls (BookCarView on mount) and is called by composite book_car (Phase 4). It must return real data before Phase 3 is complete.
Output: atomic/vehicle_service/app.py with three working routes reading from and writing to Firestore.
</objective>

<execution_context>
@/Users/chaiyichen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chaiyichen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-atomic-services/03-CONTEXT.md
@.planning/phases/03-atomic-services/03-RESEARCH.md

<interfaces>
<!-- Current stub — full contents to replace -->
<!-- Source: atomic/vehicle_service/app.py -->

Route signatures (keep exactly — only replace bodies):
  GET  /api/vehicles                    → get_vehicles()
  GET  /api/vehicles/<string:vehicle_id> → get_vehicle(vehicle_id)
  PUT  /api/vehicles/<string:vehicle_id>/status → update_vehicle_status(vehicle_id)

Firestore init pattern (keep exactly — do not change):
  try:
      import firebase_admin
      from firebase_admin import credentials, firestore as fs
      cred = credentials.Certificate(
          os.environ.get("GOOGLE_APPLICATION_CREDENTIALS", "/secrets/firebase-service-account.json")
      )
      firebase_admin.initialize_app(cred)
      db = fs.client()
  except Exception as e:
      print(f"[WARN] Firestore init failed (Phase 1 stub): {e}")
      db = None

Vehicle document shape (from seed_data.py):
  { plate_number, make, model, vehicle_type, year, status, location_lat, location_lng, branch_id }
  Document ID = plate_number (natural key)

Collection name: "vehicles"
Port: 5001
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement vehicle_service Firestore routes</name>
  <files>atomic/vehicle_service/app.py</files>
  <behavior>
    - GET /api/vehicles: returns {"status":"ok","data":[...all 10 vehicles...]} — each item includes {"id": doc.id, ...doc fields}; returns all vehicles regardless of status (frontend filters client-side)
    - GET /api/vehicles/SBA1234A: returns {"status":"ok","data":{...vehicle fields...}} with 200; returns {"status":"error","message":"Vehicle not found"} with 404 for unknown ID
    - PUT /api/vehicles/SBA1234A/status with {"status":"booked"}: returns {"status":"ok","message":"Status updated"} with 200; returns 400 if status field missing; returns 404 if vehicle not found
    - GET /api/vehicles/UNKNOWN: returns 404, not 200
    - PUT /api/vehicles/SBA1234A/status with no body: returns 400
  </behavior>
  <action>
Rewrite atomic/vehicle_service/app.py. Keep the Firestore init block and Flask app setup exactly as-is. Replace only the route bodies.

**GET /api/vehicles:**
- Guard: if db is None, return 500
- `docs = db.collection("vehicles").stream()`
- Return all vehicles: `[{"id": d.id, **d.to_dict()} for d in docs]`
- Return 200 with {"status":"ok","data":results}

**GET /api/vehicles/<vehicle_id>:**
- Guard: if db is None, return 500
- `doc = db.collection("vehicles").document(vehicle_id).get()`
- If not doc.exists: return 404 {"status":"error","message":"Vehicle not found"}
- Return 200 {"status":"ok","data":doc.to_dict()} — vehicle docs have plate_number in the data; no need to merge doc.id separately since doc.id == plate_number which is already a field

**PUT /api/vehicles/<vehicle_id>/status:**
- Guard: if db is None, return 500
- `body = request.get_json(silent=True) or {}`
- `new_status = body.get("status")`
- If not new_status: return 400 {"status":"error","message":"Missing required field: status"}
- `doc_ref = db.collection("vehicles").document(vehicle_id)`
- If not doc_ref.get().exists: return 404 {"status":"error","message":"Vehicle not found"}
- `doc_ref.update({"status": new_status})`
- Return 200 {"status":"ok","message":"Status updated"}

Add `from flask import Flask, jsonify, request` (add `request` to import — stubs only import Flask and jsonify).

Keep /health route and `if __name__ == "__main__":` block unchanged.
  </action>
  <verify>
    <automated>bash /Applications/MAMP/htdocs/y2s2/ESD/ESDProj/verify_phase3.sh 2>&amp;1 | grep -E "^  (PASS|FAIL): VEH"</automated>
  </verify>
  <done>All three VEH-* checks in verify_phase3.sh pass. GET /api/vehicles returns 10 vehicles, GET /api/vehicles/SBA1234A returns that vehicle, PUT /api/vehicles/SBA1234A/status returns ok.</done>
</task>

</tasks>

<verification>
bash verify_phase3.sh shows PASS for VEH-01, VEH-02, VEH-03. No FAIL lines for VEH-* checks.
</verification>

<success_criteria>
- atomic/vehicle_service/app.py no longer contains "Phase 1 stub" comment in any route body
- All three routes call db.collection("vehicles") with real Firestore operations
- verify_phase3.sh VEH-01, VEH-02, VEH-03 all PASS
</success_criteria>

<output>
After completion, create `.planning/phases/03-atomic-services/03-02-SUMMARY.md`
</output>
